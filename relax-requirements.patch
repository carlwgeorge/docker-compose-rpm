diff -Nurp docker-compose-1.14.0.orig/setup.py docker-compose-1.14.0/setup.py
--- docker-compose-1.14.0.orig/setup.py	2017-07-26 21:59:24.459917883 -0500
+++ docker-compose-1.14.0/setup.py	2017-07-26 22:00:29.050923458 -0500
@@ -30,16 +30,16 @@ def find_version(*file_paths):
 
 
 install_requires = [
-    'cached-property >= 1.2.0, < 2',
-    'docopt >= 0.6.1, < 0.7',
-    'PyYAML >= 3.10, < 4',
-    'requests >= 2.6.1, != 2.11.0, < 2.12',
-    'texttable >= 0.8.1, < 0.9',
-    'websocket-client >= 0.32.0, < 1.0',
-    'docker >= 2.3.0, < 3.0',
-    'dockerpty >= 0.4.1, < 0.5',
-    'six >= 1.3.0, < 2',
-    'jsonschema >= 2.5.1, < 3',
+    'cached-property >= 1.2.0',
+    'docopt >= 0.6.1',
+    'PyYAML >= 3.10',
+    'requests >= 2.6.0',
+    'texttable >= 0.8.1',
+    'websocket-client >= 0.32.0',
+    'docker >= 2.3.0',
+    'dockerpty >= 0.4.1',
+    'six >= 1.3.0',
+    'jsonschema >= 2.5.1',
 ]
 
 if sys.version_info[:2] < (3, 3):
